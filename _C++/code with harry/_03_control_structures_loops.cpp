// sequence structure
// selection structure
// loop structure
#include <iostream>
#include <conio.h>
using namespace std;

int main()
{
    system("cls");
    // if else ; else if ; else (>=18 eligible <19 not eligible)
    // switch,case,break; case (if 22 case 1: you are 22)

    /*loop in c++
    1.for loop
        for (intialisation; condition; operation)
    {
        code
    }
    2.while loop
       while (condition )
   {
        code
   }
    3.do-while loop
       do
   {
        code
   } while ( condition );
    */

    getch();
    return 0;
}