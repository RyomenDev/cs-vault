c++ Bjarne Stronohope-- extension of c
                    more control over sysytem resources +
                    memory management

variables(to hold data) = c + boolean(true / false 0r 0 / 1) types - local & global
 no use of reserved words in declartion

data types-- > built - in;
              user - defined(struct; union; enum);
              derived(array; function; pointer)

    iostream
    // input stream= direction of flow of bytes takes place from input device to the main memory
    // output stream= direction of flow of bytes takes place from the main memory to output device

    "<<"--insertion operator
    ">>"--extraction operator

    header files
    1)system header files : comes with compiler
    2)user defined : written by programmer ,produce error when not present in current directory