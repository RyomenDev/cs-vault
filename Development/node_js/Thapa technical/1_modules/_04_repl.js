const name = (myname) => {
  console.log(`my name is ${myname}`);
};

name("ben ten");


/*
REPL
: js expression
: use variables
: multiline code/loops
: use (_) to get the last results
: we can use editor mode by using .editor
    & execute using ctrl+D
*/