/*
#include <stdio.h>

int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int x, y;
        scanf("%d %d", &x, &y);
        printf("%d\n", (3 * x < 2 * y) ? 3 * x : 2 * y);
    }
    return 0;
}
*/
/*
#include <stdio.h>

int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int x;
        scanf("%d", &x);
        int z = 0.1 * x;
        printf("%d\n", (z > 100) ? z : 100);
    }
    return 0;
*/
/*
#include <stdio.h>

int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int a, b;
        scanf("%d %d", &a, &b);
        int z1 = a * 10;
        int z2 = b * 5;
        if (z1 == z2)
            printf("ANY\n");
        if (z1 > z2)
            printf("FIRST\n");
        if (z1 < z2)
            printf("SECOND\n");
    }
    return 0;
}
*/
#include<stdio.h>

int main()
{
system("cls");

getch();
return 0;
}
