#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main()
{
    system("cls");
    // int **p;
    // p = (int **)malloc(3 * size0f(3 * int *));
    int *p = (int *)malloc(sizeof(int));
    getch();
    return 0;
}